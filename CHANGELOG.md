#Changelog

##[1.0.4] - 2020-12-06
### Added
- New changelog file as CHANGELOG.md file to keep track of releases and new features or bug fixes

### Changed
- Changed from using `rand` crate to `nanorand` crate with `chacha` feature flag enabled. This reduces the numbers of dependencies for the project